# 현재 업무 인계 — 2026-08-29

이 문서는 기존 업무를 Codex 업무별 방식으로 이어가기 위한 대기 기록입니다. 이번 설정 작업에서 아래 제품 업무를 새로 시작하거나 완료한 것은 아닙니다. 새 체계의 구현·검수 담당은 아직 배정하지 않았습니다.

## 공통 선행 조건

상태는 모두 `조치필요`입니다. 기존 AO 작성자의 실제 쓰기 중지, 변경 파일 소유권 반환, 받아들일 코드·미커밋 변경과 작업 경로를 읽기 전용으로 확인해야 합니다. 해당 확인 전 새 작성자를 배정하지 않습니다. AO 작업 발행·제어·삭제·감시 재시작은 이번 범위가 아닙니다.

인계 당시 PC 원본은 `D:/petmanager`, 브랜치 `codex/marketing-war-room-poc`, HEAD `22d0b02b59483f8a32965308e24860e5643d645e`로 보고됐습니다. `src/components/admin/admin-home.tsx` 수정과 여러 미추적 자료는 기존 소유입니다. 새 제품 작업 직전에 다시 확인합니다. 새 작업본은 이번 로컬 설정과 수용된 제품 기준선이 자동 포함됐다고 가정하지 않습니다.

## 활성 업무에 전달할 연속성 delta

기존 활성 task에는 이번 문서·역할 변경이 자동 반영되지 않습니다. 각 활성 주담당에게 아래 내용만 compact handoff로 별도 전달하고 수신 확인 뒤 적용합니다.

- 대표 요청 하나당 `parent_work_id` 하나, 단계별 `step_id`·`lane`·`depends_on`·완료 조건을 기록합니다.
- lane당 active는 하나이며, 단계 종료 뒤 같은 parent의 `next-ready`를 먼저 선택합니다. 다른 업무는 명시적 우선순위 변경 또는 실제 독립 lane일 때만 병렬입니다.
- task `completed`는 상위 완료가 아닙니다. 상위 acceptance와 필요한 독립 QA PASS, P0/P1 0이 완료 조건입니다.
- `interrupted`/`failed`/10분 무출력은 stalled입니다. 같은 단계 재개 1회 후 재정체면 쓰기 중지·소유권 반환 확인 → compact handoff → 같은 work/step ID의 새 담당 교체 순서입니다.
- blocker에는 재개 조건과 `owner_approval_required: Y/N`을 기록합니다.

## 관리자 상태 5단계

- 업무 ID: `pm-admin-status-five-stage`
- 상태: `조치필요` — 기존 후보와 실제 반영·검수 기준선 확인 대기.
- 알려진 후보: `ao/petmanager-9/admin-release-readiness-safe`의 `f296f3fa`. 정규 3000 포함 여부와 독립 검수는 확인되지 않았습니다.
- 주의: 기존 3상태 도면을 최신 5단계 완료 근거로 사용하지 않습니다.
- 다음 조건: 기존 작성자 중지/소유권 확인 → 실제 현재 소스·화면 및 5단계 요구 확인 → 설계 도면과 단일 구현자 배정 → 독립 기능·화면 검수.

## 마케팅 워크룸 재설계·명칭 변경

- 업무 ID: `pm-marketing-workroom-redesign`
- 상태: `조치필요` — 최신 수용 설계, 명칭 결정, 변경 소유권 확인 대기.
- 현재 확인 수준: 완료·최종 반영을 확인하지 않았습니다. 위 PC 원본의 기존 `admin-home.tsx` 변경을 덮어쓰지 않습니다.
- 다음 조건: 종전 작성자 중지/소유권 반환과 수용된 기준선 확인 → 명칭·화면 범위를 한 업무로 확정 → 필요한 설계·구현·독립 검수 순서 진행.

## 로그인 통합·출시 차단 해소

- 업무 ID: `pm-login-release-blockers`
- 상태: `조치필요` — 실제 남은 로그인 문제와 출시 조건 확인 대기.
- 현재 확인 수준: 최신 완료 근거가 없습니다. UI 영향과 인증/원격 변경 필요 여부도 실제 조사 후 판단합니다.
- 다음 조건: 종전 작성자·기준선 확인 → 좁은 읽기 전용 재현/원인 조사 → 변경 범위와 승인 경계 확정 → 단일 구현 및 필요한 독립 검수. 원격 Auth/DB나 배포는 별도 최종 승인 전 실행하지 않습니다.

## 근거와 업데이트

초기 근거는 운영팀 작업공간의 `work/codex-subagent-setup/HANDOFF.md`입니다. 이 문서에는 새 실행 사실을 추정해 채우지 않습니다. 주담당이 실제 근거를 얻을 때 해당 업무 기록과 다음 조건만 갱신합니다. 승인·관련 문서가 꼭 필요할 때 지정 Notion 범위를 한 번 조회하며 자동 외부 기록은 하지 않습니다.
